[project]
name = "ifc-delete-guid"
version = "0.1.0"
description = "IFC GUID Batch Deletion using Excel"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ifcopenshell>=0.8.5",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
]

[project.scripts]
delete-guid = "delete_guid:main"

